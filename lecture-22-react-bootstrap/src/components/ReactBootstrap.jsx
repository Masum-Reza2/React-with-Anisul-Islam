import React from 'react'
import Button from 'react-bootstrap/Button';

// to use bootstrap with react goto react bootstrap and follow the instructionas
// npm install react-bootstrap bootstrap
// import 'bootstrap/dist/css/bootstrap.min.css'; import this css in main.jsx
// >> then start copy and pasting the components

const ReactBootstrap = () => {
  return (
    <div>
      <h1>React bootstrap</h1>
      <Button>Learn More</Button>
    </div>
  )
}

export default ReactBootstrap
